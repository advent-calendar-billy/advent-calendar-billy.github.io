# thebilly.dev

Personal blog. Posts are markdown files in `posts/`, published from `admin.html`.
Everything else on the site is stored encrypted under `vault/` and unlocked in the
browser (see `tools/build-vault.mjs`).
